name: movier
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  l: ^5.0.1
  flutter_bloc: ^9.1.1
  bloc_concurrency: ^0.3.0
  google_fonts: ^6.3.2
  dio: ^5.9.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
